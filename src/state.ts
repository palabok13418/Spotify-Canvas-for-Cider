import { ref } from "vue";

export const canvasUrl = ref("");
export const canvasActive = ref(false);
export const canvasAnalysisPending = ref(false);
export const canvasSuppressedForAppleArtwork = ref(false);
